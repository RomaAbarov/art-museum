export const enum SortBy {
  "Default" = "default",
  "Ascending" = "early artworks(asc)",
  "Descending" = "late artworks(desc)",
  "Alphabet" = "alphabet",
}

export const SortByList = [
  SortBy.Default,
  SortBy.Ascending,
  SortBy.Descending,
  SortBy.Alphabet,
];
